#include "types.h"
#include "stat.h"
#include "user.h"

int main(int argc,char *argv[])
{

if(argc<2 || argc>2)
{
return -1;
}
else
  {
printf(1,"reutrn value of trace in side test_trace= %d \n",trace(argv[1]));
    }

  exit();


}
